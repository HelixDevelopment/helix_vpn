# HelixVPN Spec — Refinement Punch-List (pass 1 → pass 2/3)

**Revision:** 2
**Last modified:** 2026-06-25T12:00:00Z

This spec set is **pass 1** of the operator's planned 2–3 refinement passes. It was
generated by a multi-agent workflow from the 16 research docs + the cross-document
synthesis. The adversarial completeness + technical-consistency review returned
**go=false** on internal-consistency grounds (the content is substantively complete;
the source-coverage ledger is accurate, not bluffed; zero placeholders/TODO/TBD). This
file records every finding so nothing is lost going into the next pass (§11.4.118 /
§11.4.134 / §11.4.6 — honest, no hidden gaps).

## Status of pass 1
- **13 spec docs + coverage ledger written** (≈11.7K lines): SPECIFICATION.md spine,
  00–11 topical/WBS docs, 99 source-coverage ledger.
- All 16 source docs cited; all open decisions D1–D7 surfaced with recommendations.
- 46 Mermaid diagrams, SQL DDL, Podman/Docker/K8s manifests embedded.

## FIXED
- **F1 — Spine navigation (was the GO-blocker).** `SPECIFICATION.md` §10 INDEX and §7
  owning-doc pointers (+ §6 and the §9 decision register) referenced an idealized filename
  scheme (`00_PRODUCT_AND_REQUIREMENTS.md` …) that does not exist on disk. Rewritten to point
  at the real files (`00-product-scope-and-principles.md` … `99-source-coverage-ledger.md`)
  with correct owning-doc mapping (Transport trait → 01; WatchNetworkMap → 02; FFI → 03).
- **F2 — Proto package unified (resolves R2).** The Coordinator service proto is now
  `helix.coordinator.v1` across `SPECIFICATION.md`, `02-control-plane.md`, and
  `11-deep-research-appendix.md` (billing stays `helix.billing.v1`). Schema-first zero-drift restored.
- **F3 — D8 added to ledger (resolves R3).** `99-source-coverage-ledger.md` decision
  cross-check now lists D1–D8 (added D8 licensing/positioning) and the header/footer say "8".

## OPEN for pass 2 (worklist)
- **R1 — Deep web research NOT performed (rate-limited).** The 10 research angles
  (wireguard, masque, hysteria2, mullvad, flutter_ffi, ios_android, go_cp, podman_k8s,
  pki_pq_nat, daita_test) all failed on the transient server rate-limit; `11-deep-research-
  appendix.md` honestly marks every external fact `UNVERIFIED (no web access)`. **Pass 2
  MUST run the deep research (§11.4.150/§11.4.99) when rate-limits clear** and replace the
  UNVERIFIED markers with cited (URL + access-date) facts; re-validate version pins.
  **Pass-2 status (2026-06-25): INTERMITTENT, not total — a 2-agent probe found the throttle is
  flaky. **RESOLVED (mostly): 9 of 10 angles now verified against real cited web sources**
  (masque, hysteria2, mullvad, flutter_ffi, ios_android, go_cp, podman_k8s, pki_pq_nat,
  daita_test — ~130 KB cited corpus in v09-research/research-*.md); `11-deep-research-appendix.md`
  rewritten from that corpus. ONLY `wireguard` remains `UNVERIFIED` (kept rate-limited) —
  re-run that one angle when the throttle next clears.**
- **R2 — Proto package name inconsistent.** ✅ RESOLVED — see F2 above.
- **R3 — Decision D8 (licensing/positioning).** ✅ RESOLVED — see F3 above.
- **R4 — doc-99 identity.** SPECIFICATION §10 originally promised "99 = consolidated
  Glossary/Decisions/References"; the actual 99 is the source-coverage ledger (glossary lives
  in SPECIFICATION §11, decision register in §9). Reconciled in F1, but pass 2 may choose to
  split a dedicated `99_GLOSSARY_DECISIONS_REFERENCES.md` if preferred.
- **R5 — Task→subtask tier asymmetry.** Phase 0 (`06-…`) formalizes a 3-tier epic→task→
  subtask breakdown; phases 1–3 (`07/08/09`) stop at epic→task (leaf tasks are concrete, so
  adequate, but asymmetric). Pass 2: bring 07/08/09 to the same subtask granularity for the
  §11.4.93 workable-items DB import.
- **R6 — Ledger-disclosed source gaps** (honestly flagged, not hidden):
  - G1 [03_ZAI] disaster-recovery / RTO-RPO not consolidated into a runbook section — OPEN,
    recommended for a Phase-2 HA/DR revision (08 or 05).
  - G2 [06_GRK] sing-box framework rejection rationale — ✅ RESOLVED: recorded in 99-ledger
    (Go framework conflicts with Rust-core D2 + iOS memory ceiling; custom Rust helix-transport
    gives byte-for-byte client↔edge reuse).
  - G3 [09_GCT] Fyne rejection rationale — ✅ RESOLVED: recorded in 99-ledger (Flutter is the
    only toolkit reaching all 8 platforms + shared design system; Fyne is desktop-only).

## Expansion waves (nano-detail volumes per MASTER_INDEX)
- **V2 Data-Plane** ✅ 13 docs (`v02-data-plane/`), review go=true. Committed `7d748b3`.
- **V3 Control-Plane** ✅ 13 docs (`v03-control-plane/`), 3 contradictions fixed to GO. Committed `5f43dae`.
- **V4 Clients** ✅ 12 docs (`v04-client/`). R7 FFI-surface contradiction RESOLVED 2026-06-25
  (helix-core-rust.md aligned to canonical ffi-surface.md). Review GO-blocker cleared.
- Citation paths normalized repo-wide: `scratchpad/kb/*` → in-repo `v09-research/*` so every
  `[research-*]`/`[SYNTHESIS]` reference resolves (one straggler: `v02-data-plane/wireguard-core.md`).

- **R7 — V4 FFI-surface contradiction. ✅ RESOLVED (2026-06-25, by hand — no agent).**
  `ffi-surface.md` is canonical owner; `helix-core-rust.md` aligned to it:
  (a) enum-name reconciled — `FfiTunnelStatus` kept as the actual Rust identifier; `ffi-surface.md`'s
  path-qualified `ffi::TunnelStatus` documented as the SAME type (note added to both docs);
  (b) `status_stream` now returns `Result<(),CoreError>` in both (+ fan-out / re-emit prose aligned);
  (c) every FFI verb returns typed `Result<_,CoreError>` (was `anyhow::Result`) — `anyhow` stays
  orchestrator-internal, converted at the boundary via `From` impls;
  (d) `CoreError` unified to the 7-variant `{NotStarted,AlreadyStarted,Config,Auth,HostFatal,BadFd,Internal}`
  (helix-core's `Busy`→`AlreadyStarted`, `TunAttach`→`BadFd`, `MapParse`→`Config`, `Orch`→folded);
  (e) `apply_map` added to `ffi-surface.md` §2 + F1 ownership row (+ `MapApplied` DTO);
  (f) footer already clean (no `scratchpad/kb/` paths). V4 review GO-blocker cleared.

## WEEKLY-LIMIT STOP (2026-06-25) → RESUMED (2026-06-25)
The account hit its weekly usage limit mid-V4; on resume the limit had cleared. R7 fixed by hand
(no agent). Operator added a CRITICAL mandate (2026-06-25): **OpenDesign (§11.4.162) must be the
design system for ALL apps, documented in full depth, and shipped as a fully decoupled reusable
submodule (`vasic-digital/helix_design`)** — all screens, all reusable components, light+dark color
schemes, the whole design system, every token form (JSON/CSS/Dart/Swift/Kotlin/ArkTS) ready for
direct incorporation across all 8 platforms. This elevates the design system from one V4 sub-doc to
its own **Volume 10 — Design System (OpenDesign)** in MASTER_INDEX. Resume order: V10 Design System
→ retry `wireguard` research → V5 Security → V6 Deploy → V1 Product → V7 Execution → V8 Testing → V0
Meta. Same batched-≤4-subagent + adversarial-review + commit-per-volume pattern (rate-limit lesson).

## Deferred deliverables (post-refinement, already agreed)
- Reusable-component **vasic-digital repos** (GitHub+GitLab) + submodules + `upstreams/` +
  `install_upstreams` — created AFTER the spec stabilizes (operator decision 2026-06-25).
- **Workable-items SQLite DB** (§11.4.93) populated from the WBS stable IDs (HVPN-P0/1/2/3-NNN)
  + **docs_chain** wiring (§11.4.106).
- **Export formats** (HTML/PDF/DOCX) for the whole spec set + Mermaid→image rendering with
  §11.4.168 visual validation.

# HelixVPN Redis Data Volume — persistent AOF data for Redis.
# Rootless Podman named volume (systemd-managed).
#
# Purpose:  Persists Redis AOF file across container recreations.
# Usage:    systemctl --user daemon-reload — volume is created automatically.

[Volume]

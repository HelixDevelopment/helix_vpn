# HelixVPN PostgreSQL Data Volume — persistent state for PG.
# Rootless Podman named volume (systemd-managed).
#
# Purpose:  Persists PostgreSQL data directory across container recreations.
# Usage:    systemctl --user daemon-reload — volume is created automatically.

[Volume]
